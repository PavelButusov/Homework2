def insertion(n):
    for i in range(1, len(n)):
        while i > 0 and n[i] < n[i - 1]:
            n[i], n[i - 1] = n[i - 1], n[i]
            i -= 1

    return n

if __name__ == '__main__':
    lst = [35,5,6,12,55,24,1]
    res = insertion(lst)
    print(res)
    
# Сложность O(n ** 2)    
# Элементы просматриваются по одному и каждый новый размещается среди уже упорядоченных
# Довольно простой метод, похож на пузырьковый с тем отличием, что элемент, который берется,
# сравнивается со всеми, что "позади".
